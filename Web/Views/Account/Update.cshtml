@using Web.Extensions
@model Web.Presentation.ViewModels.AccountViewModels.AccountViewModel

@{
    ViewBag.Title = "Update Account";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="panel panel-primary">
    <div class="panel-heading">
        <span class="fa fa-address-card-o" style="margin-right: 5px;"></span>Update Account
    </div>
    <div class="panel-body">
        @Html.AntiForgeryToken()
        @using (Html.BeginForm("Update", "Account", FormMethod.Post, new { @class = "form-horizontal", @style = "height: 360px; margin-top:20px; padding-left: 30px; padding-right: 30px;" }))
        {
            <fieldset>
                @Html.HiddenFor(a => Model.Id)
                @Html.HiddenFor(a => a.StatusId)
                <div class="col-md-4">
                    <div class="form-group">
                        @Html.LabelFor(a => Model.Name)
                        @Html.TextBoxFor(a => Model.Name, new {@class = "form-control"})
                        @Html.ValidationMessageFor(a => Model.Name, null, new {@class = "label label-danger pull-right"})
                        <br/>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(a => Model.Number)
                        @Html.TextBoxFor(a => Model.Number, new {@class = "form-control"})
                        @Html.ValidationMessageFor(a => Model.Number, null, new {@class = "label label-danger pull-right"})
                        <br/>
                    </div>
                    <div class="form-group">
                        @Html.LabelFor(a => a.SelectedPartnerId)
                        @Html.DropDownListFor(a => a.SelectedPartnerId, Model.PartnerNumberSelection, new
                        {
                            @class = "form-control js-account-parnum-select",
                            data_show_subtext="true",
                            data_live_search="true"
                        })
                        @Html.ValidationMessageFor(a => a.SelectedPartnerId, null, new {@class = "label label-danger pull-right"})
                        <br/>
                    </div>
                </div>
            </fieldset>

            <br />
            <div class="form-group pull-right">
                <input id="Save" type="submit" class="btn btn-primary pull-right" style="margin-left: 5px;" value="Save" />
            </div>
        }
    </div>
</div>

@section scripts{
    Routing.setCurrentPage("#AccountNav");
    UpdateAccountController.init();
}